1. Java & Go: ARM SDK versions don't expose vector index properties in      SQLResourcesCreateUpdateOptions 
 2. .NET:  Azure.ResourceManager.CosmosDB  v1.4.0 lacks vector types entirely (upgrade to     v1.5.0+ when available)
 3. Workaround: Use Azure CLI or portal to pre-create containers for testing